{
  "builds": [
    {
      "src": "Beardevops/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "Beardevops/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
